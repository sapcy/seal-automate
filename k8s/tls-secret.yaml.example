# TLS 인증서를 생성한 후 이 파일을 사용하여 Secret을 생성하세요
# 
# 인증서 생성 예시:
# openssl genrsa -out tls.key 2048
# openssl req -new -x509 -key tls.key -out tls.crt -days 365 -subj "/CN=seal-automate.default.svc"
#
# Secret 생성:
# kubectl create secret tls seal-automate-tls \
#   --cert=tls.crt \
#   --key=tls.key \
#   --dry-run=client -o yaml > k8s/tls-secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: seal-automate-tls
  namespace: default
type: kubernetes.io/tls
data:
  # base64 인코딩된 cert.pem
  tls.crt: <base64-encoded-cert>
  # base64 인코딩된 key.pem
  tls.key: <base64-encoded-key>

